# The deployed Cloudflare Worker. Public on purpose: the Gemini key stays a
# Cloudflare secret and the worker only answers allow-listed origins.
VITE_AI_ENDPOINT=https://naano-ai.ammtech.workers.dev
