name = "naano-ai"
main = "src/index.ts"
compatibility_date = "2026-09-01"

# GEMINI_API_KEY is never set here. It lives in Cloudflare as an encrypted
# secret: wrangler secret put GEMINI_API_KEY

[vars]
ALLOWED_ORIGINS = "http://localhost:5173,http://localhost:5174"
